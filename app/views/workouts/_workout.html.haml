%h1= @track.next_step.name
= simple_form_for([@track, @track.next_step], :html => {:class => 'form-horizontal' }) do |f|
  = f.input :emotion
  = f.input :intensity
  = f.input :sweat
  = f.input :notes
  = f.button :submit
